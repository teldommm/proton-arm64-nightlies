name: Proton ARM64 Nightly Build

on:
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:
    inputs:
      runner:
        description: 'Runner to use'
        required: false
        default: 'ubuntu-24.04'
        type: choice
        options:
          - ubuntu-24.04
          - self-hosted
      target_app_id:
        description: 'Target Android app id for hardcoded wine paths'
        required: false
        default: 'app.gamenative'
        type: choice
        options:
          - app.gamenative
          - com.winlator.cmod
      wine_ref:
        description: 'Valve wine ref (branch/tag/commit)'
        required: false
        default: 'bleeding-edge'
        type: string
      gamenative_ref:
        description: 'GameNative proton-wine ref (branch/tag/commit)'
        required: false
        default: 'proton_10.0'
        type: string
      kernel_compat_wcp_url:
        description: 'Optional URL to known-good donor .wcp for kernel32/ntdll overlay'
        required: false
        default: ''
        type: string
      kernel_compat_required:
        description: 'Fail build if kernel_compat_wcp_url is empty'
        required: false
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'true'

jobs:
  build:
    runs-on: ${{ github.event.inputs.runner || 'ubuntu-24.04' }}
    timeout-minutes: 360
    permissions:
      contents: write

    steps:
      - name: Free up disk space
        run: |
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /opt/ghc
          sudo rm -rf /usr/local/share/boost
          sudo rm -rf "$AGENT_TOOLSDIRECTORY"
          df -h

      - name: Checkout this repo
        uses: actions/checkout@v4

      - name: Set up build environment
        run: |
          sudo dpkg --add-architecture i386
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            git \
            wget \
            curl \
            unzip \
            flex \
            bison \
            gettext \
            autoconf \
            automake \
            libtool \
            pkg-config \
            mingw-w64 \
            gcc-multilib \
            g++-multilib \
            libfreetype6-dev \
            libfreetype6-dev:i386 \
            libpng-dev \
            libpng-dev:i386 \
            zlib1g-dev \
            zlib1g-dev:i386 \
            zstd \
            xz-utils \
            python3-zstandard \
            patch

      - name: Clone Wine source (ValveSoftware bleeding-edge)
        run: |
          rm -rf wine-source
          WINE_REF="${{ github.event.inputs.wine_ref || 'bleeding-edge' }}"
          git clone --depth=1 --branch "$WINE_REF" \
            https://github.com/ValveSoftware/wine wine-source \
          || {
            git clone --depth=1 https://github.com/ValveSoftware/wine wine-source
            git -C wine-source fetch --depth=1 origin "$WINE_REF"
            git -C wine-source checkout FETCH_HEAD
          }
          echo "Wine ref: $WINE_REF"
          git -C wine-source rev-parse HEAD
      - name: Clone GameNative patches and build scripts
        run: |
          GN_REF="${{ github.event.inputs.gamenative_ref || 'proton_10.0' }}"
          git clone --depth=1 --branch "$GN_REF" \
            https://github.com/GameNative/proton-wine gamenative-src \
          || {
            git clone --depth=1 https://github.com/GameNative/proton-wine gamenative-src
            git -C gamenative-src fetch --depth=1 origin "$GN_REF"
            git -C gamenative-src checkout FETCH_HEAD
          }
          echo "GameNative ref: $GN_REF"
          git -C gamenative-src rev-parse HEAD
          cp -r gamenative-src/android wine-source/android
          cp -r gamenative-src/build-scripts wine-source/build-scripts
          rm -rf gamenative-src

          # Normalize patch line endings (some upstream patches can be CRLF and
          # fail under git apply as "corrupt patch" despite valid content).
          find wine-source/android/patches -type f -name "*.patch" -exec sed -i 's/\r$//' {} +


          # Comment out patches already merged into bleeding-edge (or handled by
          # fix_*.py scripts) so they don't get double-applied or fail the build.
          python3 scripts/filter_patches.py \
            wine-source/build-scripts/build-step-arm64ec.sh \
            wine-source

          # Rewrite bare 'git apply' in the build script to use forgiving options
          # (-C1, --ignore-whitespace) and continue on failure instead of aborting.
          python3 scripts/patch_build_script.py \
            wine-source/build-scripts/build-step-arm64ec.sh

          # Apply Android-specific window.c changes (xinput2 guards, class_hints,
          # _NET_WM_PID) via script - the patch is too drifted to apply cleanly.
          python3 scripts/fix_window_c.py wine-source

          # Exclude FEX stats shm block on Android - shm_open is in libc but
          # the ntdll.so link step doesn't pull it in, causing undefined symbol.
          python3 scripts/fix_virtual_c.py wine-source

          # Apply suspend-bypass chain directly when test-bylaws patches drift.
          python3 scripts/fix_suspend_chain.py wine-source

          # Force-apply additional BYLAWS drift-prone patches and verify markers.
          echo "=== fix_test_bylaws_chain.py header ==="
          sed -n '1,40p' scripts/fix_test_bylaws_chain.py
          if ! grep -q 'Script rev: 2026-03-06-driftproof-v1' scripts/fix_test_bylaws_chain.py; then echo 'ERROR: stale fix_test_bylaws_chain.py in runner'; exit 97; fi
          python3 scripts/fix_test_bylaws_chain.py wine-source

          # Rewrite the baked-in install prefix from the Termux CI path to the
          # Winlator Cmod path that GameNative rewrites at runtime via libredirect.
          # This ensures Wine's hardcoded WINEDLLPATH points somewhere the device can find.
          APP_ID="${{ github.event.inputs.target_app_id || 'app.gamenative' }}"
          if [ "$APP_ID" = "com.winlator.cmod" ]; then
            INSTALL_DIR="/data/data/com.winlator.cmod/files/imagefs/usr/opt/wine"
          else
            INSTALL_DIR="/data/data/${APP_ID}/files/imagefs/opt/wine"
          fi
          echo "Using install_dir=$INSTALL_DIR"
          sed -i "s|export install_dir=.*|export install_dir=${INSTALL_DIR}|" wine-source/build-scripts/build-step-arm64ec.sh

      - name: Download and extract termuxfs for aarch64
        run: |
          TERMUX_DIR="$HOME/termuxfs/aarch64"
          if [ ! -d "$TERMUX_DIR/data/data/com.termux/files/usr/include" ]; then
            echo "termuxfs not found, downloading..."
            mkdir -p "$TERMUX_DIR"
            cd "$TERMUX_DIR"
            wget -q https://github.com/GameNative/termux-on-gha/releases/download/build-20260218/termuxfs-aarch64.tar
            tar -xf termuxfs-aarch64.tar
            rm -f termuxfs-aarch64.tar
          else
            echo "termuxfs already present, skipping download."
          fi

      - name: Set up Android NDK
        run: |
          NDK_DIR="$HOME/Android/Sdk/ndk/27.3.13750724"
          if [ ! -f "$NDK_DIR/toolchains/llvm/prebuilt/linux-x86_64/bin/aarch64-linux-android28-clang" ]; then
            echo "NDK not found, downloading..."
            mkdir -p "$HOME/Android/Sdk/ndk"
            cd "$HOME/Android/Sdk/ndk"
            wget -q https://dl.google.com/android/repository/android-ndk-r27d-linux.zip
            unzip -q android-ndk-r27d-linux.zip
            mv android-ndk-r27d 27.3.13750724
          else
            echo "NDK already installed, skipping download."
          fi

      - name: Set up LLVM MinGW toolchain
        run: |
          LLVM_DIR="$HOME/toolchains/llvm-mingw-20250920-ucrt-ubuntu-22.04-x86_64"
          if [ ! -f "$LLVM_DIR/bin/llvm-dlltool" ]; then
            echo "LLVM MinGW not found, downloading..."
            mkdir -p "$HOME/toolchains"
            cd "$HOME/toolchains"
            wget -q https://github.com/bylaws/llvm-mingw/releases/download/20250920/llvm-mingw-20250920-ucrt-ubuntu-22.04-x86_64.tar.xz
            tar -xf llvm-mingw-20250920-ucrt-ubuntu-22.04-x86_64.tar.xz
          else
            echo "LLVM MinGW already installed, skipping download."
          fi

      - name: Add bylaws llvm-mingw to PATH
        run: |
          echo "$HOME/toolchains/llvm-mingw-20250920-ucrt-ubuntu-22.04-x86_64/bin" >> $GITHUB_PATH

      - name: Run autogen.sh
        run: |
          cd wine-source
          bash autogen.sh

      - name: Cache wine-tools
        id: cache-wine-tools
        uses: actions/cache@v4
        with:
          path: wine-source/wine-tools
          key: wine-tools-${{ hashFiles('wine-source/configure.ac', 'wine-source/configure') }}
          restore-keys: wine-tools-

      - name: Build wine-tools
        if: steps.cache-wine-tools.outputs.cache-hit != 'true'
        run: |
          cd wine-source
          mkdir -p wine-tools
          cd wine-tools
          ../configure --without-x --without-gstreamer --without-vulkan --without-wayland
          make -j$(nproc) __tooldeps__ nls/all

      - name: Build sysvshm library
        run: |
          cd wine-source
          bash build-scripts/build-step-arm64ec.sh --build-sysvshm

      - name: Configure build
        run: |
          cd wine-source
          bash build-scripts/build-step-arm64ec.sh --configure

      - name: Verify critical patch markers
        run: |
          python3 scripts/verify_required_markers.py wine-source

      - name: Build Proton
        run: |
          cd wine-source
          bash build-scripts/build-step-arm64ec.sh --build

      - name: Install Proton
        run: |
          OUTPUT_DIR="$HOME/compiled-files-aarch64"
          STAGING="$HOME/wine-staging"

          mkdir -p "$OUTPUT_DIR/bin" "$OUTPUT_DIR/lib" "$OUTPUT_DIR/share" "$STAGING"

          cd wine-source
          make install DESTDIR="$STAGING" -j$(nproc)

          echo "=== Staging tree top-level dirs ==="
          find "$STAGING" -maxdepth 8 -type d | head -40

          # Find where 'make install' actually put the wine binary.
          # The prefix is baked into the build by build-step-arm64ec.sh and may vary
          # (e.g. /data/data/com.winlator.cmod/files/imagefs/usr/opt/wine).
          WINE_BIN=$(find "$STAGING" -type f \( -name "wine64" -o -name "wine" \) | grep -v "\.so\|wine-preloader\|wineserver\|winecfg\|wineboot\|winedbg\|winepath\|winefile\|winemine\|winemapi\|winebrowser\|wineconsole\|winevulkan" | head -1)
          if [ -z "$WINE_BIN" ]; then
            echo "ERROR: wine binary not found anywhere in staging tree"
            exit 1
          fi
          STAGED="$(dirname "$(realpath "$WINE_BIN")")/.."
          STAGED="$(realpath "$STAGED")"
          echo "=== Found wine install root: $STAGED ==="

          echo "=== STAGED bin contents ==="
          ls -la "$STAGED/bin/" || echo "(empty)"

          cp -r "$STAGED"/bin/wine*   "$OUTPUT_DIR/bin/" 2>/dev/null || true
          cp -r "$STAGED"/bin/reg*    "$OUTPUT_DIR/bin/" 2>/dev/null || true
          cp -r "$STAGED"/bin/msi*    "$OUTPUT_DIR/bin/" 2>/dev/null || true
          cp -r "$STAGED"/bin/notepad "$OUTPUT_DIR/bin/" 2>/dev/null || true
          cp -r "$STAGED/lib/wine"    "$OUTPUT_DIR/lib/"
          cp -r "$STAGED/share/wine"  "$OUTPUT_DIR/share/"

          echo "=== OUTPUT_DIR bin contents after copy ==="
          ls -la "$OUTPUT_DIR/bin/"

          # Box64 looks for 'wine' in bin/ ? Wine normally installs a 'wine' shell
          # wrapper. If it's missing (only wine64 present), create a symlink.
          if [ ! -e "$OUTPUT_DIR/bin/wine" ] && [ -f "$OUTPUT_DIR/bin/wine64" ]; then
            ln -s wine64 "$OUTPUT_DIR/bin/wine"
            echo "Created wine -> wine64 symlink"
          elif [ -e "$OUTPUT_DIR/bin/wine" ]; then
            echo "wine present: $(file "$OUTPUT_DIR/bin/wine")"
          else
            echo "ERROR: neither wine nor wine64 found in $OUTPUT_DIR/bin/ ? build incomplete"
            exit 1
          fi

          echo "=== Final OUTPUT_DIR bin contents ==="
          ls -la "$OUTPUT_DIR/bin/"
          echo "=== wine binary type ==="
          file "$OUTPUT_DIR/bin/wine"
          file "$OUTPUT_DIR/bin/wine64" 2>/dev/null || true

      - name: Download prefixPack.txz
        run: |
          wget https://github.com/GameNative/bionic-prefix-files/raw/main/prefixPack-arm64ec.txz \
            -O $HOME/compiled-files-aarch64/prefixPack.txz

      - name: Apply kernel32 compatibility overlay (optional)
        run: |
          COMPAT_URL="${{ github.event.inputs.kernel_compat_wcp_url || '' }}"
          REQUIRED="${{ github.event.inputs.kernel_compat_required || 'false' }}"

          WORK_DIR="$HOME/kernel-compat-donor"
          rm -rf "$WORK_DIR"
          mkdir -p "$WORK_DIR/extracted"

          if [ -z "$COMPAT_URL" ]; then
            echo "No donor URL provided, resolving latest GameNative Proton arm64ec WCP..."
            COMPAT_URL="$(python3 -c 'import json,re,urllib.request; data=json.load(urllib.request.urlopen("https://api.github.com/repos/GameNative/proton-wine/releases/latest")); assets=data.get("assets",[]); c=[a.get("browser_download_url","") for a in assets if re.match(r"^proton-[0-9].*arm64ec\.wcp$", a.get("name",""))]; print(c[0] if c else "")')"
          fi

          if [ -z "$COMPAT_URL" ]; then
            if [ "$REQUIRED" = "true" ]; then
              echo "ERROR: unable to resolve kernel compatibility donor WCP URL"
              exit 1
            fi
            echo "No donor found, skipping overlay"
            exit 0
          fi

          echo "Using donor WCP: $COMPAT_URL"
          wget -q "$COMPAT_URL" -O "$WORK_DIR/donor.wcp"
          tar -xf "$WORK_DIR/donor.wcp" -C "$WORK_DIR/extracted"

          python3 scripts/apply_kernel_compat_overlay.py \
            "$HOME/compiled-files-aarch64" \
            "$WORK_DIR/extracted"
      - name: Generate profile.json files and package
        run: |
          OUTPUT_DIR="$HOME/compiled-files-aarch64"
          GIT_HASH=$(git -C wine-source rev-parse --short HEAD)
          GIT_DATE=$(git -C wine-source log -1 --format='%cd' --date=format:'%Y%m%d')
          # versionName must end in -arm64ec so GameNative recognises it as arm64ec
          # and extracts the .wcp. Use date+hash as build identifier before the suffix.
          VERSION="proton-10.0-nightly-${GIT_DATE}-${GIT_HASH}-arm64ec"

          mkdir -p "$GITHUB_WORKSPACE/output"

          # versionCode: always 0 ? avoids ambiguity when GameNative reconstructs
          # the entry name as verName+"-"+versionCode (date already in verName)
          VERSION_CODE=1

          # GameNative prepends "proton-" to versionName to build the install dir path,
          # so versionName in profile.json must NOT include the "proton-" prefix.
          # e.g. VERSION=proton-10.0-nightly-...-arm64ec
          #   -> PROFILE_VERSION=10.0-nightly-...-arm64ec
          #   -> installs to imagefs/opt/proton-10.0-nightly-...-arm64ec/
          PROFILE_VERSION="${VERSION#proton-}"

          # Proton profile ? for standard Winlator / GameNative
          python3 scripts/generate_profile.py \
            "${OUTPUT_DIR}/profile.json" \
            "${PROFILE_VERSION}" \
            "${VERSION_CODE}" \
            "Proton 10 ARM64 nightly ${GIT_DATE} (${GIT_HASH})"

          cd "$OUTPUT_DIR"
          tar --zstd -cf "$GITHUB_WORKSPACE/output/proton-${VERSION}.wcp" \
            bin lib share prefixPack.txz profile.json

          # Wine profile ? for Winlator CMOD / Ludashi
          python3 "$GITHUB_WORKSPACE/scripts/generate_profile.py" \
            "${OUTPUT_DIR}/profile.json" \
            "${PROFILE_VERSION}" \
            "${VERSION_CODE}" \
            "Proton 10 ARM64 nightly ${GIT_DATE} (${GIT_HASH})" \
            wine

          tar --zstd -cf "$GITHUB_WORKSPACE/output/proton-wine-${VERSION}.wcp" \
            bin lib share prefixPack.txz profile.json

          cd "$GITHUB_WORKSPACE"
          sha256sum output/proton-${VERSION}.wcp > output/proton-${VERSION}.wcp.sha256
          sha256sum output/proton-wine-${VERSION}.wcp > output/proton-wine-${VERSION}.wcp.sha256

          ls -lh output/

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: proton-arm64-nightly
          path: output/
          retention-days: 30

      - name: Create GitHub Release
        run: |
          GIT_HASH=$(git -C wine-source rev-parse --short HEAD)
          GIT_DATE=$(git -C wine-source log -1 --format='%cd' --date=format:'%Y%m%d')
          VERSION="proton-10.0-nightly-${GIT_DATE}-${GIT_HASH}-arm64ec"
          TAG="nightly-${GIT_DATE}-${GIT_HASH}"
          gh release create "$TAG" output/* \
            --title "Proton 10 ARM64 Nightly (${GIT_DATE})" \
            --notes "Built from ValveSoftware/wine bleeding-edge @ ${GIT_HASH} with GameNative Android + ARM64EC patches. Automated nightly ? use at your own risk." \
            --prerelease \
            || gh release upload "$TAG" output/* --clobber
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Update latest.json
        run: |
          GIT_HASH=$(git -C wine-source rev-parse --short HEAD)
          GIT_DATE=$(git -C wine-source log -1 --format='%cd' --date=format:'%Y%m%d')
          VERSION="proton-10.0-nightly-${GIT_DATE}-${GIT_HASH}-arm64ec"
          WCP_SHA=$(cut -d' ' -f1 output/proton-${VERSION}.wcp.sha256)
          WCPXZ_SHA=$(cut -d' ' -f1 output/proton-wine-${VERSION}.wcp.sha256)
          python3 scripts/generate_latest.py \
            "latest.json" \
            "${VERSION}" \
            "${GIT_DATE}" \
            "${GIT_HASH}" \
            "proton-${VERSION}.wcp" \
            "${WCP_SHA}" \
            "proton-wine-${VERSION}.wcp" \
            "${WCPXZ_SHA}" \
            "nightly-${GIT_DATE}-${GIT_HASH}"
          cat latest.json

      - name: Commit latest.json
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add latest.json
          GIT_DATE=$(git -C wine-source log -1 --format='%cd' --date=format:'%Y%m%d')
          git diff --staged --quiet || git commit -m "chore: update latest.json for nightly ${GIT_DATE}"
          git pull --rebase origin HEAD
          git push














